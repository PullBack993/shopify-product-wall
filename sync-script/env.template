# Shopify Store Configuration
# Your Shopify store URL (without https://)
# Example: your-store.myshopify.com
SHOPIFY_STORE_URL=your-store.myshopify.com

# Your Shopify Private App Access Token
# Go to: Settings > Apps and sales channels > Develop apps > Create an app
# Grant permissions: read_products, read_inventory
SHOPIFY_ACCESS_TOKEN=your-access-token-here

# Sync Schedule Configuration
# Cron format: minute hour day-of-month month day-of-week
# Default: Every 2 weeks on Sunday at 2 AM (0 2 */14 * 0)
# Examples:
#   - "0 2 */14 * 0" - Every 2 weeks on Sunday at 2 AM
#   - "0 2 1,15 * *" - 1st and 15th of every month at 2 AM
#   - "0 2 * * 0" - Every Sunday at 2 AM
SYNC_SCHEDULE=0 2 */14 * 0

# Run sync immediately on startup (true/false)
RUN_ON_START=false

# Enable the scheduler (true/false)
# Set to false to disable automatic syncing
ENABLE_SCHEDULER=true

# Timezone for the scheduler
# Use IANA timezone names: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=Europe/Vienna

# Node.js Environment
NODE_ENV=production

# Docker Timezone
TZ=Europe/Vienna

# Optional: Custom product label tags
# Comma-separated list of tags that indicate special product types
# SALE_TAGS=sale,discount,promo,clearance,reduced
# NEW_TAGS=new,latest,fresh,recent
# TOP_SELLING_TAGS=bestseller,top-selling,popular,trending 